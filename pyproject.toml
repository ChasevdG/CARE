[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "care"
version = "0"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["care"]
